#!/usr/bin/env bash
set -euo pipefail

script_dir="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
repo_root="$script_dir"

cd "$repo_root"

dotnet build "Sources/ffrwd/ffrwd.csproj" >/dev/null
exec dotnet run --no-build --project "Sources/ffrwd/ffrwd.csproj" -- "$@"
